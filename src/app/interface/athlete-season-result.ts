export interface AthleteSeasonResult {
  name: string;
  totalPoints: number;
  totalKm: number;
  league: number;
  rank: string;
  isPromoted: boolean;
  isRelegated: boolean;
  isLeader: boolean;
}
